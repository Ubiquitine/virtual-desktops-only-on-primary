for (var i = 0; i < workspace.screens.length; i++) {
    var screen = workspace.screens[i];
    print("Screen #" + i + ": Name: " + screen.name + ", Brand: " + screen.manufacturer + ", Model: " + screen.model);
}
